__pycache__/
*.py[cod]

# Состояние мониторинга: открытые инциденты и счётчики за сутки.
# Файл рабочий, у каждого развёртывания свой.
monitor_state.json

# Секреты. ВНИМАНИЕ: bot/.env уже попал в историю репозитория раньше —
# одного этого правила недостаточно, токен нужно отозвать в @BotFather.
.env
